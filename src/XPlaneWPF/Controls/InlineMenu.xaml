<ItemsControl x:Class="XPlaneWPF.Controls.InlineMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             MaxHeight="200"
             >
    <Grid>
        <!--
        <Button Style="{StaticResource FlatMenuItem}">[Previous Menu]</Button>

        <DockPanel HorizontalAlignment="Right" Grid.Column="1">
            <Button Style="{StaticResource FlatMenuItem}">Data</Button>
            <Button Style="{StaticResource FlatMenuItem}">Queries</Button>
            <Button Style="{StaticResource FlatMenuItem}">Models</Button>
            <Button Style="{StaticResource FlatMenuItem}" IsEnabled="False">Execute</Button>
        </DockPanel>-->
    </Grid>
</ItemsControl>
