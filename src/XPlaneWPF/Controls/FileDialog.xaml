<UserControl x:Class="XPlaneWPF.Controls.FileDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>        
        <StackPanel  Orientation="Horizontal">
            <ListBox x:Name="drivesListBox" ItemsSource="{Binding Drives}" SelectionChanged="drivesListBox_SelectionChanged"/>
        
            <ListBox x:Name="dirListBox" ItemsSource="{Binding Directories}"/>
            
            <ListBox x:Name="fileListBox" ItemsSource="{Binding Files}"/>
        </StackPanel>
    </Grid>
</UserControl>
